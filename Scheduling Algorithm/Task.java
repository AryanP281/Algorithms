class Task {
    
    int id; //The id of the task
    double weight; //The weight i.e priority of the task
    double length; //The duration of the task

    Task(int id, double weight, double length)
    {
        this.id = id;
        this.weight = weight;
        this.length = length;
    }
}